{
  "name": "shoppingonline",
  "version": "1.0.0",
  "description": "Shopping Online - Full Stack Application",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run admin\" \"npm run customer\"",
    "server": "cd server && npm start",
    "admin": "cd client-admin && npm start",
    "customer": "cd client-customer && npm start",
    
    "install-all": "npm install && cd server && npm install && cd ../client-admin && npm install && cd ../client-customer && npm install",
    "build": "cd client-customer && npm run build",
    "start": "cd server && node index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "swiper": "^12.1.3"
  }
}
